# Staff Portal - Production Environment Variables
# These variables are embedded into the React build at compile time

# API Base URL - The backend server URL
REACT_APP_API_URL=https://staff.yourdomain.com

# Socket.IO Server URL (for real-time notifications)
REACT_APP_SOCKET_URL=https://staff.yourdomain.com

# Environment
NODE_ENV=production
